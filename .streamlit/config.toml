# Streamlit asks for an email address and collects usage statistics on first
# run. Neither is needed for a personal project, and declining here stops it
# prompting again on a new machine.
[browser]
gatherUsageStats = false
